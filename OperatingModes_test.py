def test_safeMode():
    # Verify only tasks required to run in Safe Mode are running 
    # ensure all running tasks are killed except for idle 
    pass

def test_detumbleMode():
    # Verify only tasks required to run in Detumble Mode are running
    # Send detumbling command to ADCS
    # Set the main task to sleep so only idle task is running
    # Starts 
    pass


